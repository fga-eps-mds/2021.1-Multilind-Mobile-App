import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  goback: {
    position: 'absolute',
    marginTop: '7%',
    marginLeft: '5%',
    // backgroundColor: 'red',
  },
});

export default styles;
